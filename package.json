{
  "name": "gridsome-portfolio",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "sslset":"set NODE_OPTIONS=--openssl-legacy-provider",
    "serve": "set NODE_OPTIONS=--openssl-legacy-provider && gridsome develop"

  },
  "dependencies": {
    "@gridsome/plugin-google-analytics": "^0.1.2",
    "@gridsome/plugin-sitemap": "^0.4.0",
    "@gridsome/source-filesystem": "^0.6.2",
    "bootstrap": "^5.2.3",
    "bootstrap-vue": "^2.23.1",
    "gridsome": "^0.7.0"

  },
  "devDependencies": {
    "sass": "^1.56.2",
    "sass-loader": "^10.1.1"
  }
}
